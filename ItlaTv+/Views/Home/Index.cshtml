@using Application.ViewModels
@model List<SeriesViewModel>
@{
    ViewData["Title"] = "Home Page";
}

<div class="container">
    <div class="row justify-content-center mt-5">
        <form class="d-flex" asp-controller="Series" asp-action="Search" method="post">
            <input class="form-control me-1" type="search" placeholder="Search by name" aria-label="Search" name="searchTerm">
            <button class="btn btn-primary" type="submit">
                <i class="fa-solid fa-magnifying-glass"></i>
            </button>
        </form>
    </div>
</div>

<div class="container mt-5">
    <h2>Series Actuales</h2>

    <form asp-controller="Series" asp-action="FilterByProducer" method="post">
        <select name="producerId" class="form-select mb-3">
            <option value="">All Producers</option>
            @foreach (var series in Model)
            {
                <option value="@series.Producer?.Id">@series.Producer?.Name</option>
            }
        </select>
        <input type="submit" value="Filter" class="btn btn-primary">
    </form>

    <div class="row">
        @if (Model == null || Model.Count == 0)
        {
            <h3>No se encontraron series.</h3>
        }
        else
        {
            @foreach (var series in Model)
            {
                <div class="col-md-4">
                    <div class="card mb-4 bg-dark text-white">
                        <img src="@series.ImagePath" class="card-img-top" alt="Series Image">
                        <div class="card-body">
                            <h5 class="card-title">@series.Name</h5>
                            <p class="card-text">
                                @if (series.Genres != null && series.Genres.Count > 0)
                                {
                                    @series.Genres.First().Name
                                    @if (series.Genres.Count > 1)
                                    {
                                        <text> - @series.Genres.ElementAt(1).Name</text>
                                    }
                                }
                            </p>
                            <p class="card-text"><small class="text-muted">@series.Producer?.Name</small></p>
                            <a asp-controller="Series" asp-action="Details" asp-route-id="@series.Id" class="btn btn-primary">Detalles</a>
                        </div>
                    </div>
                </div>
            }
        }
    </div>
</div>
